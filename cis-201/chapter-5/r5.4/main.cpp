True or False

1. A function has exactly one return statement. // True

2. A function has at least one return statement. // False

3. A function has at most one return value. // True

4. A function with return value void never has a return statement. // False

5. When executing a return statement, the function exits immediately. // True

6. A function with return value void must print a result. // False (It may not print any result)

7. A function without arguments always returns the same value. // False (The function may use Instance/Global variables)